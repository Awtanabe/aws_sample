%h1 Categories#index
%p Find me in app/views/categories/index.html.haml


= link_to "カテゴリー作成", new_category_path
- @categories.each do |category|
  = category.name
  = link_to "削除", category_path(category), method: :delete, class: 'btn btn-danger'