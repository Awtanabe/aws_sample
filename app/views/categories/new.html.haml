%h1 Categories#new
%p Find me in app/views/categories/new.html.haml


= form_for @category do |f|
  %label 親タグ
  .form-group
    = f.collection_select :parent_id, Category.where(user_id: current_user.id), :id, :name, { include_blank: '選択してください' }
  .form-group
    %label タグ名
    = f.text_field :name
  = f.submit '保存', class: 'btn btn-primary'